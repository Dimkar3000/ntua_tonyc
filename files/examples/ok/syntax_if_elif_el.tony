def main(): 
int a, b
bool c,d
<*
Its if close has an error. Fix them to test next one
*>
    a:= 5
    b:= 4
    c:= true
    if a <= b: 
        puti(a)
    elsif not c:
        puti(b)
    elsif d = true:
        putb(c)
    elsif 'a' = 'c':
        putb(c)
    else :
        putc('a')
    end 

end